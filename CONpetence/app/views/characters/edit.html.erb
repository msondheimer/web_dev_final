<h2> <%= "#{@char.name} cosplyaing tips" %> </h2>

<%= form_tag "/characters/#{@char.id}", :method => "patch" do %>

		
<div>
				<p class="text-muted"> 

		<%= text_area_tag :costume, @char.cosplaying, rows: 5, cols: 40 %> </p></div> 
	<div> <%= submit_tag "Done editing", class: "btn btn-success"%> </div>
<%end %>